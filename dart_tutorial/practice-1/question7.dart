// Write a program to find quotient and remainder of two integers.


void main(List<String> args) {
  const divisor = 4;
  const dividend = 24;
  print('Quotient ($divisor / $dividend) : ${dividend/divisor}');
  print('Remainder ($divisor % $dividend) : ${dividend%divisor}');
}